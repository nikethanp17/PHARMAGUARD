# ── Server ────────────────────────────────────────
PORT=4000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# ── MongoDB ───────────────────────────────────────
# Local:  mongodb://127.0.0.1:27017/pharmaguard_auth
# Atlas:  mongodb+srv://<user>:<pass>@cluster.mongodb.net/pharmaguard_auth
MONGODB_URI=mongodb://127.0.0.1:27017/pharmaguard_auth

# ── JWT Secrets (change these!) ───────────────────
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=CHANGE_ME_ACCESS_super_secret_key_at_least_32_chars
REFRESH_TOKEN_SECRET=CHANGE_ME_REFRESH_super_secret_key_at_least_32_chars
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# ── Email (Ethereal auto-created in dev if left blank) ────
# Production: set SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@yourgenes.health

# ── Security ──────────────────────────────────────
BCRYPT_ROUNDS=12
